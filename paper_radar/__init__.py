"""paper-radar · 面向计算机方向的文献检索 / 推荐 / 每日简报系统。

设计原则（对应"可扩展、少硬编码"）：
  1. 数据源、LLM 提供方、邮件通道都是**插件注册表**，新增实现=新增一个文件；
  2. 所有业务参数（关键词权重、分区表、提示词、查询式）都在 config.example.json 里，
     代码中只有"保底默认值"，没有硬编码的研究方向；
  3. 核心零第三方依赖，标准库即可跑起来。
"""

__version__ = "1.0.0"
__all__ = ["__version__"]
